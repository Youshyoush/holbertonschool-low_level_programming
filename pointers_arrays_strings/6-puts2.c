#include "main.h"

/**
 * puts2 - prints every 2 character of a string.
 * @str: string pointer.
 *
 */

void puts2(char *str)
{
	int len = 0, j;

	while (len != '\0')
	{
		len++
	}
	for (j = 0; j % 2 = 0; j++)
	{
		if (j == 0)
		{
			_putchar(*str);
		}
		_putchar(str[j]);
	}
	_putchar('\n');
}
