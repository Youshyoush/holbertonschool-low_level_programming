#include "main.h"

/**
 * _islower - check if the character is a lowercase
 * @c: The character to check (ASCII value)
 *
 * Return: 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

